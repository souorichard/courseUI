export interface Course {
    id: number;
    namecourse: string;
    period: string;
}

export interface Student {
  id: number;
  namestudent: string;
  age: number;
}

export interface Teacher {
  id: number;
  nameteacher: string;
}
